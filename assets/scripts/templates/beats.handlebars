{{#each beats}}


{{> partial}}

    <!-- <h1> <small> {{name}}</small></h1> -->
    <!-- <div> -->
      <!-- {{#if desc}}
        {{desc}}
      {{else}}
        No author.
      {{/if}} -->
    <!-- </div> -->

  {{/each}}
